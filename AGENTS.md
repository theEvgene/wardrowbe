## Agent skills

### Issue tracker

Issues and specifications live in GitHub Issues for `theEvgene/wardrowbe`. Implementation is pushed directly to `main`; feature branches and pull requests are not used during the MVP stage. See `docs/agents/issue-tracker.md`.

### Domain documentation

Wardrowbe is currently treated as a single-context repository. Use the root `CONTEXT.md` and root `docs/adr/` when present. See `docs/agents/domain.md`.
